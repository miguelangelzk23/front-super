import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.superm.app',
  appName: 'front-super',
  webDir: 'dist/front-super/browser'
};

export default config;
